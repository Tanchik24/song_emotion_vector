version: "3.8"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8002:8002"
#  frontend:
#    build:
#      context: demo
#      dockerfile: demo/Dockerfile
#    ports:
#      - "8080:8080"
